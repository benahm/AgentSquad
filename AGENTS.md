# AGENTS.md

## Mermaid CLI

- You can use `mmdc` to render Mermaid diagrams.
- On this project, `mmdc` must be executed through `cmd.exe`.
- Preferred command pattern:

```cmd
cmd.exe /c mmdc -i "path/to/input.mmd" -o "path/to/output.svg"
```
